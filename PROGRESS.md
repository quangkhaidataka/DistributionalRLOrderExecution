# PROGRESS.md — DisRL project state

> Living status doc for the DisRL thesis work. **Update this whenever meaningful progress is made** (task finished, batch run, blocker hit). Newest state at the top of each section. Dates are absolute. Companion: **DECISION.md** (why), **PLAN.md** (the plan), **RUNBOOK.md** (run commands), **VERIFY.md** (checks), **PAPER_FIXES.md** (paper audit).

**Last updated:** 2026-08-23
**Active branch:** `fix/unify-arch-jd-recalibration` (off `main`) · **Design-v2 work on `feature/design-v2`**
**Repo:** `/Users/user/Desktop/DisRL` · conda env `finrl_env` (py3.11, torch 2.2.2, numpy 2.4.6) · MPS available.

> **Design-v2 (branch `feature/design-v2`, gov. doc `PLAN_V2.md`):** B1–B4 code COMPLETE + logic-tested (see PLAN_V2 decision log). **B5 Pipeline-1 (AC study) RUN & COMPLETE (2026-07-19)** — 5 seeds {42,123,7,2024,31} × (DQN, DDQN, IQN-neutral 2×20k) trained N=20 at CPU, per-seed 1,200-CRN selection → 10k test (11 rows). Tables in `results/_v2_ac/` (T-AC-1 per-seed, T-AC-2 `_aggregate/ac_seed_summary.{txt,tex}`, T-AC-3 α-ladder). **Verdict: all pre-registered AC-sanity expectations HELD** — all agents ≈ equal (learned mean 1.73–1.84, CVaR₉₅ 2.43–2.48 bps, overlapping within seed-std), α-ladder FLAT (Mean/CVaR₉₅ vary < across-seed std over α∈{.3,.5,.7,.9,.95,1}), cap-frac ≈ 0 for all except MaxSpeed (=1.0); TWAP≡AC. AC confirms the Gaussian null (no CVaR benefit) — the intended sanity check. Ops note: harness reaps background jobs on a ~34-min window, so each seed ran as staged sub-jobs (DQN/DDQN, IQN 0:20k, IQN 20k:40k, assemble) via an idempotent driver; the proven `--episodes a:b` resume made reaps free to re-run. **B5 Pipeline-2 PHASE A (regime calibration scan) RUN & COMPLETE (2026-07-19)** — 2×2 grid σ_high∈{.002,.004}×p₀₁∈{.05,.10}, seed 42, 40k eps, per cell {TWAP eval, DDQN, IQN-neutral 2×20k staged} → 10k eval + α-ladder + criteria (a)–(d) + feature-scale. `run_v2_regime_scan.py` got additive reap-safe staging (`--stage {ddqn,iqn_a,iqn_b,assemble}`; results-neutral). **All 4 cells HARD-PASS (a,b,c); RECOMMENDED (0.002,0.05)** — largest α=0.95 gap +0.947 and the only cell with all-positive & largest α-ladder gaps. Outputs in `results/_v2_regime/_scan/` (`scan_summary.{txt,csv}`, per-cell metrics.json, action-vs-spread heatmap for the recommended cell). Feature-scale: Δp* std ~3–7e-4 vs q*/t* ~0.2–0.3 (price channel suppressed); the action-vs-spread heatmap is ~FLAT (IQN trades ≈0.1·q0 across all spread bins) → the policy barely responds to spread. **Phase-A decisions (user, 2026-07-20):** cell LOCKED (0.002,0.05) → `results/_v2_regime/locked_cell.json`; σ̂ (`use_rv_feature`) ENABLED (6-D; IQN 11851/DQN·DDQN 5579); feature_scale stays OFF. **σ̂ confirmation pilot (round 2/2, seed 42, IQN-neutral 40k)** in `results/_v2_regime/seed42/logs/` (`pilot_sigma_ablation.{txt,json}`, 3 heatmaps). **Finding — σ̂-on is a mixed/negative result:** CVaR₉₅ improves 7.90→5.42 bps (−31%) BUT via a faster near-constant liquidation (~0.2·q0/step, front-loads in ~5 steps; mean IS worsens 1.93→2.65) that IGNORES both spread and σ̂ per-step (action-vs-σ̂ heatmap FLAT) and NEUTRALIZES the CVaR distortion (α-ladder gaps → 0.000 vs σ̂-off's +0.5…+1.2). Single seed — could be training variance; the CVaR-benefit story lives in the σ̂-OFF arm. Flagged for the user's Phase-B σ̂ go/no-go. **HARD STOP after the pilot report; Phase B on explicit go only.** B4 TAQ run phase still user-triggered. v1 locked results frozen at tag `locked-main-results`. **σ̂ DECISION REVERSED (user, 2026-07-20):** the main study is **σ̂-OFF (5-D; param guard IQN 11787 / DQN·DDQN 5515)** — enabling σ̂ neutralized the CVaR-α distortion (ladder gaps → 0) via faster open-loop liquidation, erasing the headline. `results/_v2_regime/locked_cell.json` set `use_rv_feature=false`; the σ̂-on pilot is archived to `results/_v2_regime/_ablation_sigma_on/` as **ablation arm E**; `feature_scale` stays OFF. **B5 Pipeline-2 PHASE B (full regime study, σ̂-off) RUN & COMPLETE (2026-07-20)** — RegimeJumpEnv(0.002,0.05), 5 seeds {42,123,7,2024,31} × (DQN 40k, DDQN 40k, IQN-neutral 2×20k) staged → per-seed 1,200-CRN selection (min val-CVaR₉₅) → 10k test (12 rows) + stress-vs-calm breakdown. Tables in `results/_v2_regime/seed*/logs/` + `_aggregate/` (T-RG-1…4, `regime_seed_summary.{txt,tex}`, `regime_phaseB_report.txt`). **VERDICT — one strong result, one null:** (1) ✅ **Distributional RL cuts the tail and is the most stable.** IQN-neutral CVaR₉₅ **5.26±0.09 bps vs TWAP 12.79±0.43 (−59%)**, tightest across-seed spread [5.17,5.40] AND lowest CVaR₉₅ of all learned agents (DDQN 5.64±0.35, DQN 6.15±0.78) — IQN learns the best + most reproducible tail-aware liquidation. (2) ❌ **The CVaR-α inference dial is INERT at this scale.** 5-seed-mean α-ladder gaps ≈0 (α.5 +0.035, α.7 +0.002, α.9 +0.182, α.95 +0.003) and α=0.3 is NEGATIVE (−0.347±0.49, sign-flips at 3/5 seeds); 0/5 seeds monotone; the scan's rosy +2.41@α.3 was a final-weights + single-seed artifact (proper CVaR-selection erases it). No Mean↑/CVaR↓ tradeoff — τ-truncation barely moves either. (3) ⚠️ Adaptation weak: action-vs-spread mean-action range 0.042 (trades ~0.13–0.17·q0) → tail benefit ≈ SPEED (fast liquidation to cap in stress), not fine spread-adaptation. Reading (proposal): flat ladder is more consistent with a state-UNPREDICTABLE tail (jumps arrive exogenously in stress) than with net underfit (IQN-neutral itself is not underfit — it cuts tail −59% and is the most stable) → d=128 unlikely to help; user's call. Headline for the thesis = "IQN > scalar RL on tail + stability," NOT "free monotone CVaR dial." **B5 Pipeline-3 (TAQ AAPL 2014, σ̂-off) RUN & COMPLETE — now 5 SEEDS {42,123,7,2024,31} (2026-07-21)** — walk-forward train Jan–Jul / val Aug–Sep / test Oct–Dec; q0=5000, η=1e-5 (gate OK, impact 5×), N=20 3-min bars, 50k eps/seed (DQN/DDQN 1 job, IQN 2×25k); per-seed 1,200-CRN selection → 10k test (12 rows). run_v2_taq.py seed-namespaced → `results/_v2_taq/AAPL/seed{S}/`; aggregate in `_aggregate/` (`taq_seed_summary.{txt,tex}`, `taq_multiseed_report.txt`); seed-42 detail = pre-registered primary. **VERDICT vs 3 pre-registered claims (5-seed mean±std, headline = aggregate NOT best seed):** (1) ✅ **IQN beats scalar RL on the tail** — IQN-neutral CVaR₉₅ **26.24±1.04 < DDQN 26.81±0.77 < DQN 34.23±6.04**; CVaR₉₀ **19.88 < DDQN 20.41 < DQN 26.18** (IQN best CVaR₉₀ of ALL agents); **−54% vs TWAP 56.9**. vs MaxSpeed: IQN beats it on CVaR₉₀ (19.88<20.05) but ties/loses on CVaR₉₅ (26.24 vs 26.07, Δ+0.16). Mechanism = fast liquidation, cap-frac seed-sensitive (0.12–0.996, mean 0.73). (2) ✅ **Risk-neutral IQN already captures the cut** — best distorted α improves neutral by only +0.47 bps (1.8% of tail), within noise. (3) ✅ **CVaR-α dial adds NO systematic tail control — 3rd env** — 5-seed-mean gaps +0.22…+0.47 bps but all <2σ (|mean|/std 0.31–1.31), non-monotone, sign-flip at the full-dump seed 42 (all α) + seed 31; point estimate mildly +ve on TAQ (vs flat AC / ≈0 Regime) but not dependable. **REPRODUCIBILITY nuance (honest):** IQN across-seed CVaR₉₅ std 1.04 ≪ DQN 6.04 ✅ but **NOT < DDQN 0.77** ❌ — on real data DDQN & MaxSpeed (std 0.40) are *more* stable than IQN (unlike Regime where IQN was most stable); IQN's wider spread is driven by seed-varying liquidation speed (cap-frac). Best/worst IQN seed 31 (25.20) / 2024 (27.90), range 2.70 — headline stays the mean 26.24. **Reading:** on real AAPL the cap-constrained tail-optimal policy is fast liquidation (60-min drift ≫ 5× impact); IQN learns it and beats both scalar agents on the tail, but does not dominate the trivial MaxSpeed dump and is less seed-stable than DDQN. Echoes v1 Batch-E (TAQ=IL). Cross-env result consistent across all 3 envs: **IQN's value = learning the tail-aware policy; the inference-time α-distortion adds no systematic tail control.** **ALL THREE v2 pipelines COMPLETE (AC / Regime 5-seed / TAQ 5-seed).** **REGIME JUMP-ROBUSTNESS SWEEP RUN & COMPLETE (2026-07-23)** — additive `--jump-std/--jump-intensity` on run_v2_regime.py (defaults byte-identical; overrides → `results/_v2_regime/_robustness/sJ{js}_lJ{ji}/seed{S}/`). 3 points × 3 seeds {42,123,7}, all else = locked cell: A(σ_J=0.08,λ=0.10 mild), B(σ_J=0.32,λ=0.10 harsh), C(σ_J=0.16,λ=0.05 rare). Tables in `_robustness/*/` + `_robustness/_aggregate/robustness_summary.{txt,tex}`. **HEADLINE win-count across 14 point×seed cells (BASE 5-seed + A/B/C 3 each):** IQN-neutral tail < **DQN** in **11/14 on CVaR90 AND 11/14 on CVaR95 (sign-test p=0.029 each)** — IQN's edge over DQN is ROBUST across jump regimes. IQN < **DDQN** in **11/14 on CVaR90 (p=0.029)** but only **8/14 on CVaR95 (p=0.395, ≈coin-flip)** — the advantage over DDQN holds on the upper tail but is NOT robust on the *deep* CVaR₉₅ tail once jumps are perturbed (DDQN strong at base 4/5, cell-specific elsewhere). **No dump-collapse** at any point (learned Std>0.05, cap<1). Boundary behaviour as designed: mild(A)/rare(C) jumps → small tail (TWAP CVaR95 7.1/8.0, A flagged <8), all agents cluster ~4.1–4.4, IQN edge shrinks toward parity; harsh(B) jumps → large tail (TWAP 23.6, flagged >20), IQN 7.5 & DDQN 8.3 both beat DQN 12.1. **MaxSpeed (fast dump) is competitive-to-best at every point** (A 4.27 / B 7.31 / C 4.28 / BASE 5.27) — consistent with the jump-driven-tail story. Verdict: IQN robustly dominates DQN across the sweep; vs DDQN the win is upper-tail-only and cell-specific on the deep tail; the fast-dump baseline remains hard to beat. **ALL v2 EMPIRICS COMPLETE (3 pipelines + robustness sweep).** **TAQ CROSS-SECTION extended to GOOG + MSFT, 5 seeds each (2026-07-24)** — generalised the data build (data-driven split detection: GOOG 2:1 @2014-04-03, MSFT none; T-TAQ-0 clean), η-gate (per-ticker; default q0=5000/η=1e-5 TRANSFERS — GOOG MaxSpeed impact 0.226 bps / MSFT 2.79 bps, both 5×, in band), and run_v2_taq.py (`--ticker`, AAPL byte-identical). Tables in `results/_v2_taq/{GOOG,MSFT}/seed*/` + `_aggregate/{GOOG,MSFT}_seed_summary.*`, `cross_ticker_summary.*`. **CROSS-SECTIONAL VERDICT (5-seed mean):** (1) ✅ **IQN robustly beats DQN** — AAPL 26.2, GOOG 27.4, MSFT 23.1 CVaR₉₅; pooled **IQN<DQN 14/15 name-seed cells (p=0.0005)**; −54/−80/−50% vs TWAP. (2) ⚠️ **IQN vs DDQN is a wash** — pooled 9/15 (p=0.30); IQN edges DDQN on the mean for AAPL/GOOG, ties MSFT, but not reliable per-seed. (3) **vs MaxSpeed is NAME-DEPENDENT** — ties the trivial dump on AAPL (26.2 vs 26.1) & MSFT (23.1 vs 23.0), but **CRUSHES it on GOOG (27.4 vs 130.1, −79%)**: on GOOG (high price ~$558, high intraday vol) fast dumping is catastrophic and the learned distributional policy is ESSENTIAL — the strongest single-name IQN win in the whole study. (4) **α-ladder INERT on both new names** (gaps ~0, within noise) — now consistent across AAPL/GOOG/MSFT + Regime + AC (5 environments). Reproducibility: IQN most stable on GOOG (std 0.64 < DQN 3.18, DDQN 2.33) but not on MSFT/AAPL (DDQN tighter). Transparency: headline = aggregate; GOOG best/worst seed 31/7 (26.3/28.1), MSFT 2024/7 (22.1/24.9). **Cross-section synthesis: the "IQN > scalar DQN" claim generalises cleanly; "IQN dominates the trivial dump" is FALSE in general but SPECTACULARLY TRUE on GOOG; the α-dial is universally inert.** **GOOG NEGATIVE-MEAN-IS DIAGNOSTIC (eval-only, 2026-07-24) → VERDICT (A) TIMING CONFOUND — REVISES the GOOG headline.** The GOOG learned agents show mean IS ≈ **−35 bps** (they appear to *make* money) vs TWAP/MaxSpeed ≈ 0 and AAPL ≈ +1. Diagnostic in `results/_v2_taq/_diagnostics/GOOG/goog_diagnostic.{txt,tex}`: **CHECK 1** — the negative mean is ~100% the **timing term** (IQN −35.6 = timing −36.0 + temp-impact +0.21 + penalty +0.22; impact/penalty tiny-positive and IDENTICAL to AAPL's +1.1) → NOT cheaper execution, it's **price-timing P&L**. **CHECK 2** — GOOG agents HOLD inventory far longer (remain ~0.2–0.4 mid-episode) vs AAPL front-loading to 0.10 by step 4. **CHECK 5** — GOOG Oct–Dec drift std **119.5 bps/ep** (AAPL 40, MSFT 35). **CHECK 3** — negative mean does NOT collapse under detrend (−36.7) or day-reversal (−34.8) ⇒ exploited signal is intraday **volatility/mean-reversion** (agent sells into observed up-moves via Δp\*), not removable linear drift — confound is pervasive. **CHECK 4** — IS math exact (recon Δ 9e-13), no look-ahead (perturbing bar 8 leaves steps 0–7 identical), single code path, no residual split → **NO bug/leakage**. **CONSEQUENCE: DEMOTE GOOG from a headline execution win to a Limitations/caveat** — the tail "win" over MaxSpeed (27 vs 130) reflects timing EXPOSURE on a volatile name, not cheaper liquidation; TWAP/MaxSpeed can't time. **AAPL/MSFT comparisons remain valid** (impact-dominated, timing ≈0). Net cross-section claim reverts to the robust one: **IQN > scalar DQN on the tail across names (14/15, p=0.0005); the "IQN beats the trivial dump on GOOG" claim is a timing confound and must be caveated.** **ALL v2 EMPIRICS COMPLETE (3 pipelines + robustness sweep + 3-name cross-section + GOOG diagnostic). HARD STOP — next is paper surgery (main_paper.tex, user-triggered), NOT more runs.** **STRATEGY / SCHEDULE EVIDENCE ADDED (eval-only, 2026-08-22)** — supervisor comment on the chapter: *"there is no discussion on the strategy"* (aggregate risk metrics only, never the learned schedule). New self-contained folder **`strategy_analysis/`** (`rollout_strategies.py`, `plot_strategies.py`, `README.md`, `figure_snippet.tex`): replays the study's own protocol (CRN 1,200-ep val selection → min val-CVaR₉₅ → 10k test at seed+99,999, per-agent RNG reseed, `share_iqn_weights`) on the EXISTING checkpoints — **no retraining, `results/` read-only** — and logs per-period `q_t`, `x_t`, the observed spread and the true regime flag. **Reproduction verified: re-derived Mean IS and CVaR₉₅ match `logs/all_results.json` to 6 d.p. for all 7 agents on all 5 seeds, both Regime and TAQ-AAPL.** Figures (PNG 300 dpi + PDF) in `strategy_analysis/figures/`. **FINDINGS (5 seeds × 10k eps):** (1) **every learned agent front-loads hard** — Regime t₅₀ (period by which half the block is sold) IQN **2.12** / DDQN 2.28 / DQN 2.30 vs TWAP≡AC 10.0; AAPL IQN **2.06** / DDQN 2.09 / DQN 2.81. The learned policy is a *capped fast dump*, not a refined trajectory — which is exactly why MaxSpeed (t₅₀=2.0) is so hard to beat and why the α-dial has nothing to grip. (2) **IQN's edge is a CLEANER finish, not a different shape** — Regime t₉₀ IQN **3.82** vs DDQN 4.84, DQN 7.63; IQN leaves **0.0%** for the forced terminal liquidation vs DDQN 1.4%; on AAPL IQN 3.76 / DDQN 3.82 / DQN 9.36 (DDQN≈IQN, matching the "IQN≈DDQN" wash). (3) **IQN is the most seed-reproducible schedule** — opening trade x₀ = 0.235±0.012 (Regime) and 0.248±0.004 (AAPL) vs DQN ±0.037/±0.064. (4) **The schedules are essentially OPEN-LOOP** — splitting each decision *within period t* by observed spread (narrow vs wide) or by the true hidden regime moves IQN's traded quantity by ≤0.0008·q₀ (Regime; max single-period gap) and 0.011·q₀ (AAPL); t₅₀ shifts ≤0.01 periods between stress-hit and calm episodes. **This is the mechanism behind the α-dial null**: the tail benefit comes from an unconditional choice of *speed*, and the CVaR-α distortion moves the schedule by ≤0.0003·q₀ (Regime) — inert in schedule space too. Numbers for the discussion paragraph in `strategy_analysis/strategy_summary.txt`; ready-to-paste `\begin{figure}` blocks + draft captions in `strategy_analysis/figure_snippet.tex`. **ORDER-SIZE ROBUSTNESS SWEEP RUN & COMPLETE (2026-08-23)** — new self-contained folder **`order_robust_check/`** (every module a copy, AAPL bars a copy, `_guard_paths()` refuses any write outside the folder; the main `results/` was never touched). Question: does IQN keep its tail advantage as the parent order grows? AAPL TAQ walk-forward, **η = 1e-5 FIXED at every level** (never rescaled with q₀, so impact cost grows through η·x), q₀ ∈ {5k, 50k, 250k, 1M} shares (0.016 / 0.155 / 0.777 / 3.108% of 2014 ADV 32,178,605), agents {TWAP, DQN, DDQN, IQN-neutral}, 12,000 train eps/agent (main study 50k), main-study protocol otherwise (1,200-ep CRN val selection at min val-CVaR₉₅ over 12 checkpoints → 10k held-out Oct–Dec test, env+global RNG reseeded per agent). **5 seeds {42,123,7,2024,31} at 50k/250k/1M; 3 seeds {42,123,7} at 5k.** Tables in `order_robust_check/results/table_order_robust.tex` + `results/q0_*/level_summary.{json,txt}`; narrative in `order_robust_check/SUMMARY.md`. **VERDICT — the tail advantage is SIZE-LIMITED, but IQN degrades gracefully.** CVaR₉₅ cut vs TWAP: q₀=5k **DQN −41.4 / DDQN −51.3 / IQN −50.9%**; 50k **−21.3 / −38.4 / −31.4%**; 250k **−8.1 / −7.5 / −7.8%** (all three within noise, sds 1.3–2.7); 1M **+5.4 / +9.0 / −0.1%**. (1) The advantage roughly halves the tail at child-order size and decays monotonically, vanishing by ~0.8% of ADV. (2) IQN does NOT lead below the top level — DDQN is best at 5k and 50k — consistent with the cross-section's "IQN vs DDQN is a wash on AAPL." (3) **The result is at 3.1% of ADV:** IQN-neutral recovers TWAP (CVaR₉₅ **103.30 ± 1.74 vs TWAP 103.35 ± 1.49**, mean IS 46.80 vs 46.22) while **DQN +5.4% (108.95) and DDQN +9.0% (112.63, across-seed sd 8.54, mean IS 61.75 = 34% above TWAP)** blow past it. At seed 31 the selected IQN checkpoint matches TWAP to the fourth decimal (46.22135 vs 46.21990 bps). **Mechanism:** under linear temporary impact total cost is η·Σx², minimised by equal splits — so once impact dominates, the impact-optimal schedule *is* TWAP, and the hard front-loading all three agents learn at small size (see `strategy_analysis/`) is exactly the wrong reflex. IQN can unlearn it; DQN and DDQN cannot. **Thesis framing: do NOT claim the tail advantage persists — claim graceful degradation** (the learned policy collapses onto the correct benchmark rather than diverging from it). Caveats: reduced 12k budget, uneven seed count (3 at 5k), linear impact (`impact_estimation/` found real AAPL impact concave |Δp| ∝ |flow|^0.75, so 1M is a stress test not a calibrated scenario). **Ops note:** the sweep lost power mid-run at q₀=1M; seeds 2024 AND 31 were both incomplete (IQN-neutral stalled at ep10000, no metrics.json). `run_cell` calls `train_segment(..., 0, EPISODES, EPISODES)`, and full resume state is only persisted when `ep_end < total_episodes` — so **no resume file is ever written here** and an interrupted agent restarts from ep 0 (deterministic and protocol-identical, just not free). Both seeds re-run 2026-08-23 (~13 min IQN retrain each, DQN/DDQN skipped via their existing `ep12000.pt`), then `--summarize` regenerated all four level summaries + the LaTeX table.

---

## Current status: Batches A/B1/C/E COMPLETE, D dropped → final paper-number handoff

**Batch E DONE** (cheap CPU review follow-ups; `results/_batch_e_report.md`):
- **E1 — Immediate-Liquidation baseline** (`ImmediateLiquidationAgent` + `run_il_baseline.py`, AC/JD/TAQ → `results/_il_baseline/`): IL=(2.084,2.084) in AC/JD (= DDQN's JD collapse); **on TAQ IQN-CVaR₀.₉₅ ≡ IL exactly** — the headline TAQ CVaR win is a dump-at-t₀ policy.
- **E2 — DDQN jump-sensitivity rows** (`run_jump_sensitivity.py --ddqn-level`, new subdirs): DDQN dumps at all 3 λ (dump 0.965/0.999/0.999), even at low λ where IQN-neutral does not.
- **E3 — selection-rule appendix** (`run_selection_appendix.py`, eval-only, 1200-ep CRN val, 5 seeds → `results/_selection_appendix/`): **DDQN's dump-collapse is a CVaR-selection artifact — it un-dumps under mean-IS selection at 5/5 JD seeds** (Std ~4.4, dump 0.000). Tail-based checkpoint selection *picks* the dump checkpoint; the agent isn't intrinsically degenerate. Main tables stay cvar-selected; this is appendix material.

Batch D (width ablation) dropped (not blocking). Two findings feed the paper narrative (PAPER_FIXES §4.3): the TAQ headline = IL, and the DDQN collapse = selection artifact.

<details><summary>previous status: Batches A/B1/C complete — paper-number handoff (kept)</summary>

**Locked JD config: λ_J=0.05, σ_J=0.16, μ_J=0, cvar-selection.** Batches **A** (seed-42 headline + TAQ), **B1** (AC+JD × 5 seeds, staged CPU + aggregated), and **C** (jump sensitivity re-centred on the locked cal + impact-misspec 3×3) are all done and green. Consolidated results: `results/_batch_bc_report.md`; aggregates in `results/_aggregate/`, `results/jump_sensitivity_summary.*`, `results/impact_misspec_*/`.

**Headline findings:** (1) **DDQN dump-collapses at 5/5 JD seeds** (dump-signature CVaR₉₀=CVaR₉₅; Std<0.05 at 2/5) — the reclassified scalar-vs-distributional finding, now at full seed coverage. (2) **Differentiation Δ(α=0.95) = +0.098 ± 0.226 bps, positive 3/5 seeds** — the zero-cost CVaR effect is real on average but small and seed-dependent (dominated by the one seed where IQN-neutral isn't already tail-aggressive). (3) **Jump sensitivity:** low λ → IQN under-hedges (high tail); base λ=0.05 → sweet spot (−73% vs TWAP); high λ → IQN itself dumps → locked λ is the non-degenerate regime. (4) **Impact misspec:** all agents robust, CVaR₉₅ scales smoothly ±2×; η=γ=1.0 reproduces the locked table. (5) AC is a clean Gaussian sanity check (CVaR gives no benefit). Next: hand numbers to PAPER_FIXES WILL-CHANGE rows (list below / in the final report).

### ▶ Next step
1. Populate PAPER_FIXES WILL-CHANGE rows from the aggregated tables (JD/AC/TAQ DQN·DDQN rows; jump-param rows; JD prose with the *honest* weak-differentiation framing). Do NOT edit main_paper.tex.

</details>

<details><summary>superseded status: staged JD retrain + R.4 gate FAIL (kept for history)</summary>

### staged full-scale JD retrain DONE → R.4 (original rule) FAILED → resolved by the 2×2 diagnostic + rule amendment

Locked **(λ_J=0.05, σ_J=0.16)** and ran the 30k-ep JD retrain **staged** (one agent per job) via `experiments/run_jd_staged.py` — validated byte-identical to the monolithic path (equivalence smoke) and to split `--eval-agents`. **Ran on CPU, not MPS:** the MPS DQN job was reaped at ep16000/30000; CPU is ~6× faster here (DQN 164s, DDQN 183s, IQN-neutral 484s — all ≪ 34 min). Order-independent per-agent RNG reseed added to `train_agent`+`evaluate_all` makes staged ≡ monolithic and CPU deterministic.

**R.4 gate = FAIL.** Table at `results/_seeds/seed42/jump_diffusion/logs/comparison_table.txt`; diagnostics at `logs/gate_diagnostics.json`.
- ✅ Meaningful tail (TWAP CVaR₉₅ 12.63 ∈ [4,15]); IQN-neutral cuts tail to 3.46 (−73% vs TWAP); IQN-neutral dump 0.000; DQN/IQN/IQN-CVaR Std > 0.05; param guard 11462/5190.
- ❌ **DDQN degenerate** — dump-at-t0 fraction 0.999 → Std IS 0.0092 < 0.05 (**triggers the FALLBACK RULE**: any learned agent Std < 0.05).
- ❌ **No differentiation at headline α** — IQN-CVaR₀.₉₅ CVaR₉₅ (3.483) not < IQN-neutral (3.461); sign flips across eval seeds (within noise). α-sweep shows a reduction only at aggressive α=0.5 (3.35 vs neutral 3.54).

**Resolution (2026-07-17):** the 2×2 diagnostic (D1 σ=0.16 selection test; D2 σ=0.12 fallback retrain; `results/_jd_diagnostics/decision_matrix.md`) showed all four cells fail the *original* rule, driven by DDQN's σ/selection-independent collapse. User locked **σ=0.16-cvar** and **amended the rule** (DDQN collapse → reported finding; IQN-only env-health criteria). See the ★ section in DECISION.md.

</details>

---

## Done (chronological)

### Onboarding / planning
- **CLAUDE.md** written (repo guide). **PLAN.md** + **PAPER_FIXES.md** written and user-approved: verified findings F1–F4, decisions D1–D4, robustness suite R1–R5, run schedule, and a full `main_paper.tex` audit (paper never edited directly).

### Phase 1 — code fixes (branch, one commit per task)
- **P1-T0** unblock `envs` imports (dropped moved `lobster_env`). **On this branch `from envs import ...` works.**
- **P1-T1/T2** unify DQN/DDQN width 128→64; add `agents/param_utils.py` param-count guard. **IQN = 11,462 trainable params, DQN/DDQN = 5,190.** Guard wired into all build sites.
- **P1-T3** fix jump-intensity bug (was `poisson(λ·dt)` with dt=12 → ~18 jumps/ep; now per-period) + recalibrate.
- **P1-T4** config hygiene: YAML marked reference-only, matched to dataclass defaults.
- **P1-T5** fix stale comments/tests: **state is 5-D (not 6-D), action space is 6 (not 5)** — paper was right, code was stale.
- **P1-T6** remove per-env architecture branch in `eval_simulation.py`.
- **P1-T7** add `--device {cpu,mps}` (default cpu) to `run_simulation.py` + `run_tag.py`.
- **N9** delete dead half-spread block in `taq_env.py` (fill = `mid - eta*x_t`, behavior unchanged).

### Phase 2 — robustness infrastructure (branch)
- **P2-T1** `run_seeds.py` (multi-seed) + `aggregate_seeds.py` (mean±std, LaTeX booktabs) + `exp_utils.py` (config dump, non-empty-dir guard) + `tests/test_aggregate.py`.
- **P2-T2** `sweep_cvar_alpha.py` (CVaR-α frontier, eval-only).
- **P2-T3** `run_jump_sensitivity.py`. **P2-T4** `run_impact_misspec.py`. **P2-T5** `run_width_ablation.py`.
- **P2-T6** `--smoke` flags + shared `dump_config_json`.

### Verification fixes (surfaced by VERIFY smoke runs)
- `evaluation/visualizer.py`: `tensor.numpy()` → `.tolist()` (numpy 2.x + torch 2.2.2 ABI break).
- `tests/test_agent.py` (stale module renames + numpy) and `tests/test_baseline.py` (stale 6/5 dims, N=10→5, vacuous DQN/DDQN assertion) repaired. **test_agent 27/27, test_baseline 76/76, run_tests 32/32.**

### Batch A executed (seed 42, MPS, ~2.7 h) — see gate report
- Re-ran AC + JD (`results/_seeds/seed42/…`), TAQ DQN/DDQN retrain (`results/taq/AAPL_seed42/`, kept IQN per D2), 3 CVaR sweeps.
- **TAQ headline intact:** IQN-CVaR₀.₉₅ CVaR₉₅ 6.13 / Max 6.24 → 88.8% / 97.0% reduction vs TWAP.
- **JD found DEGENERATE:** adverse μ_J=-0.30 made immediate liquidation dominant → every agent dump-at-t0, **IQN Std IS = 0.000, IQN-CVaR ≡ IQN-neutral** (no differentiation). Also: JD IQN training unstable (best ep14000, degraded by ep30000); the sweep grabbed the wrong (newest) checkpoint.

### JD recalibration — code only (T1–T5, branch, committed)
- **T1** env → **symmetric μ_J=0**, provisional λ=0.05/σ=0.12 (both SimConfig + DEFAULT_SIM_CONFIG); docstring rewritten with pre-registered acceptance criteria.
- **T2** `scan_jump_calibration.py` — scans λ∈{.05,.10}×σ∈{.08,.12,.16}, scores hard criteria (non-degeneracy, dump<50%, TWAP CVaR₉₅∈[4,15]) + soft (differentiation), recommends a cell.
- **T3** `run_tests.py` Section 7 — computed P-band `1-exp(-λN)±0.03` + symmetric assertion (32/32).
- **T4** `sweep_cvar_alpha.py` — select **best-by-val-CVaR₉₅** checkpoint (not newest) + `--ckpt-dir`.
- **T5** RUNBOOK "JD recalibration" section + `run_seeds.py --jump-intensity/--jump-std` overrides.

### Staged full-scale JD retrain executed (seed 42, CPU) — R.4 gate FAILED
- `run_jd_staged.py` (STEP 1): `--only-agent`/`--assemble-eval`/`--eval-agents`; equivalence smoke = byte-identical to monolithic + split. Order-independent RNG reseed in `train_agent`+`evaluate_all`; `--checkpoint-freq` added.
- Trained DQN (best ep8000)/DDQN (ep13000)/IQN-neutral (ep7000), 30k eps, CPU (MPS DQN was reaped at ep16000 → archived `results/_archive_jd_partial_mps_reaped_seed42/`; CPU ~6× faster).
- **Gate FAIL:** DDQN dump-collapse (Std 0.0092, dump 0.999) → fallback trigger; no headline-α differentiation (noise). Fallback (0.05,0.12) recommended, NOT launched.

### JD calibration scan executed (seed 42, MPS)
- Added `--cell λ σ` / `--summarize` / `--out-root` to `scan_jump_calibration.py` so the ~1 h grid runs **one cell per short job** (the harness reaps long background jobs at ~34 min); whole-grid path unchanged. Committed.
- Ran all 6 cells (λ∈{0.05,0.10}×σ∈{0.08,0.12,0.16}, μ=0), ~4–5 min each, all exit 0 → `results/_jump_scan/summary.{txt,csv}`.
- **Hard-pass cells: (0.05,0.08), (0.05,0.12), (0.05,0.16)**; λ=0.10 all fail (dump ≥70% at σ≤0.12; TWAP CVaR₉₅=17.5>15 at σ=0.16). **RECOMMENDED (0.05, 0.16)** (largest gap +0.068). Differentiation (d, soft) holds for (0.05,0.12) and (0.05,0.16). Awaiting user confirmation before R.3.

---

## Not done / open

- **DECISION: run the pre-declared fallback (0.05, 0.12)?** — R.4 failed at (0.05,0.16) (DDQN dump-collapse + no headline differentiation). Fallback rerun is *recommended, not launched* (per FALLBACK RULE). Awaiting user. If run and it still doesn't differentiate at α=0.90/0.95, discuss alternatives (not auto-tune) — see DECISION.md.
- **Batch B / C / D** (multi-seed, sensitivity/misspec, ablations) — **blocked** until the JD table passes non-degeneracy AND differentiation.
- **`PAPER_FIXES.md` number updates** after retrains (JD table, DQN/DDQN rows, abstract percentages) — WILL-CHANGE rows still placeholders. Note: **unified DQN (hidden=64) is worse than the old 128** on TAQ (CVaR₉₅ 19.7 vs 6.30) — feed to the fair-comparison narrative.
- **`main_paper.tex`** — never edited directly; all changes catalogued in PAPER_FIXES.md.
- **N7 (non-split-adjusted AAPL data)** — flagged, not fixed (R-6); test-window results not corrupted, but note for the final data pipeline.

## Operating notes
- **Long training runs must run in the user's own terminal** (harness reaps background jobs at ~34 min). Use `caffeinate -i`. See RUNBOOK for exact commands.
- **Never overwrite `results/`** — archive only. New runs go to new suffixed dirs; `run_seeds` refuses non-empty dirs.
